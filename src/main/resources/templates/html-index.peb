<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns="http://www.w3.org/1999/html" xml:lang="en" lang="en">
<head>
    <title>{{report.title}}</title>
    <style>
        body {
            margin-top: 0;
            font-family: Tahoma, Arial, sans-serif;
        }

        li a {
            text-decoration: none;
            list-style-position: inside;
            font-size: large;
            color: slategray;
        }

        li::marker {
            color: limegreen;
        }
    </style>
</head>
<body>
<main role="main">
    <h1>LSD Reports</h1>
    <ul>
        {% for report in capturedReports %}
        <li><a href="{{report.path.fileName}}">{{report.title}}</a></li>
        {% endfor %}
    </ul>
</main>
</body>
</html>
